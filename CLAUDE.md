# output-toolkit 项目规范

## 一、基础编码强制规范

1. 遵循**阿里巴巴Java开发手册（泰山版）**所有约束，包含命名、常量、注释、异常、数据库、并发、集合、IO、接口规范等，违规代码必须主动修正并标注修改点。
2. 代码分层清晰，严格区分 Controller / Service / Mapper / DTO / VO / Entity / Util / Config / Exception 分层，职责单一，禁止跨层直接调用。
3. 控制代码行数、方法长度：单个方法尽量不超过80行（非严格），单个类不超1000行，过长自动拆分并说明拆分思路。
4. 入参校验统一使用 JSR303 注解，业务异常统一自定义异常类，禁止裸抛 `RuntimeException`/`Exception`。
5. 所有SQL禁止 `select *`，分页、索引、事务、锁场景必须给出配套说明。

## 二、设计模式使用规范

1. 但凡业务场景适配，**优先选用标准23种GoF设计模式**，禁止随意硬编码堆砌逻辑；无合适模式时需明确说明「当前场景无需设计模式+替代优化方案」。
2. 使用任意设计模式时，配套完整三段式说明注释，放在类头部文档注释中：
   - ① 使用原因：当前业务痛点是什么，为什么选用该模式而非其他模式；
   - ② 模式收益：解决了哪些痛点、扩展性/复用性/解耦带来的具体好处；
   - ③ 完整类结构：列出该模式下全部角色类、每个类承担的职责、类之间依赖关系。
3. 模式代码示例尽量极简通用，同时贴合当前业务场景，杜绝脱离业务的空洞Demo；关键调用链路增加行内注释说明模式流转逻辑。
4. 多种模式组合使用时，分别拆解每种模式的三段说明，补充模式间协同逻辑。

## 三、Java 高阶特性使用规范

允许且推荐合理使用 Java8+ 高阶特性做简化优化，有业务价值的优先，不允许无意义过度嵌套、可读性崩坏的滥用。

### 推荐使用特性清单
- Lambda 表达式、Stream 流式处理（集合筛选、分组、求和、去重、转换、分页）
- 函数式接口：Consumer、Supplier、Predicate、Function、UnaryOperator、Bi 系列接口
- Optional 空值安全处理，彻底替代多层 if 判空
- LocalDateTime、LocalDate、Duration、Period 全新时间 API，废弃 Date/SimpleDateFormat
- 方法引用、构造器引用、静态方法引用
- 接口默认方法、静态接口方法、函数式接口自定义封装
- CompletableFuture 异步多线程流式编排，替代传统 ThreadPoolExecutor 手写线程逻辑

### 约束边界
- 超长链式 Stream、多层嵌套 Lambda、一次性写百行流式代码不允许，过长需拆分工具方法；
- 使用每一处 Java8+ 高级特性，必须配套注释说明：该特性解决什么传统代码痛点、相比循环/旧写法优势在哪；
- 复杂流式逻辑拆分后，单独封装工具方法，保留简洁主业务代码。

## 四、注释统一规整标准

### 1. 类注释（必须）
- 使用 JavaDoc `/** */`，包含：类功能、适用场景、作者、创建时间；
- 若使用设计模式，追加上文规定的「原因+好处+角色类」三段说明。

### 2. 方法注释（所有 public/protected 方法强制，私有复杂方法补充）
- 标注：功能描述、入参含义、返回值含义、抛出异常场景、业务边界限制；
- 复杂业务流程，分点简述执行步骤，不用冗余废话，直击逻辑。

### 3. 行内单行注释 `//`
- 不注释显而易见的基础语法，只注释**业务逻辑分支、特殊判断、复杂算法、兼容逻辑、坑点**；
- 注释放在代码上方，不放在行尾拥挤堆砌；
- 说明「为什么这么写」，而非复述代码做了什么。

### 4. 常量/枚举注释
- 枚举每个值单独注释业务含义；
- 魔法数字全部抽常量，常量注释说明业务来源与使用场景。

### 5. DTO/VO/Entity 字段注释
- 使用 Swagger `@Schema`（SpringDoc）或 `@ApiModelProperty` 注解 + 字段注释，标注字段用途、取值范围、特殊默认值。

## 五、日志规范

### Controller 日志前缀
- Controller 接口及其调用的 Service 链路中所有 `log.info` 日志，需添加**统一业务前缀**。
- 前缀命名规则：根据接口业务含义使用简短中文描述，如 `用户信息查询_`、`订单创建_`。
- 前缀需贯穿 Controller → Service → DAO 整条调用链路的 `log.info`。

### 日志级别使用
- `log.info`：正常业务流程的关键节点
- `log.warn`：可恢复的异常或降级场景
- `log.error`：需要关注的错误（必须包含异常堆栈）

## 六、异常处理规范

- 任何 catch 块不允许空处理，不允许吞掉异常不打印日志。
- 所有 `log.error` 必须包含异常对象作为参数，以便打印完整堆栈。
- 即使需要向上层重新抛出异常，也需先 `log.error` 记录原始异常信息。
- 对于不可恢复的异常，记录 error 后抛出业务异常；对于可降级的异常，记录 warn 并执行降级逻辑。

## 七、交付输出要求

每次生成代码输出结构固定：
1. 前置说明：场景分析 + 是否使用设计模式 + 模式完整三段说明
2. 完整分层代码（带全套规范注释）
3. 文末学习总结：知识点、拓展优化建议

## 八、附加学习辅助规则

1. 新增代码/重构代码后，末尾附带一段精简总结：本次代码用到的设计思想、可复用知识点、可拓展优化方向。
2. 出现复杂逻辑、框架特性、设计模式关键点时，主动补充简单拓展知识点，帮助理解底层原理。
3. 代码尽可能低耦合，每一处特殊处理都留下注释解释业务背景，隔一段时间回看也能看懂业务上下文。
4. 禁止简化注释、省略设计模式说明，若空间有限优先精简代码，保留完整注释学习信息。
